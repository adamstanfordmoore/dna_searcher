### NOT FULLY WORKED ON
### TRYING TO USE PRE_BUILT DOCKER
build:
  docker:
    web: Dockerfile
run:
    web: python manage.py makemigrations && python manage.py migrate && gunicorn DNA_form.wsgi:application --bind 0.0.0.0::8000